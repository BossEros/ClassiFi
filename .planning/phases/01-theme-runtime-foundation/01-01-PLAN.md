---
phase: 01-theme-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/index.html, frontend/src/main.tsx, frontend/src/shared/store/useThemeStore.ts, frontend/src/index.css]
autonomous: true
requirements: [THM-03, THM-04]
user_setup: []

must_haves:
  truths:
    - "Theme preference persists across reloads via localStorage."
    - "App startup applies the saved theme without a visible flash."
  artifacts:
    - path: "frontend/index.html"
      provides: "Early theme bootstrap before React render"
      contains: "classifi-theme"
    - path: "frontend/src/shared/store/useThemeStore.ts"
      provides: "Single source of truth for theme preference"
    - path: "frontend/src/index.css"
      provides: "No-transition bootstrap guard and minimal root theme overrides"
  key_links:
    - from: "frontend/index.html"
      to: "document.documentElement"
      via: "bootstrap script sets theme class"
      pattern: "document\\.documentElement"
---

<objective>
Implement early theme bootstrap and persistence so the saved theme applies before React render without visible flash.

Purpose: Satisfy THM-03 and THM-04 with a stable runtime foundation for later visual work.
Output: Bootstrap script, store initialization alignment, and minimal root theme styles.
</objective>

<execution_context>
@C:/Users/Christian Dave Vilan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Christian Dave Vilan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-theme-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-theme-runtime-foundation/01-RESEARCH.md

@frontend/index.html
@frontend/src/main.tsx
@frontend/src/shared/store/useThemeStore.ts
@frontend/src/index.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add early theme bootstrap before React render</name>
  <files>frontend/index.html</files>
  <action>Insert a small inline script in the document head that reads localStorage key `classifi-theme`, falls back to `light` when missing or invalid, sets the `light`/`dark` class on `document.documentElement`, and applies a temporary `no-transition` class. Guard localStorage reads with try/catch to avoid errors in restricted environments.</action>
  <verify>Reload the app with localStorage set to `light` and `dark`; confirm the correct class is present on the html element before React renders.</verify>
  <done>Inline bootstrap sets `light`/`dark` class and `no-transition` without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Align runtime store initialization with bootstrap</name>
  <files>frontend/src/shared/store/useThemeStore.ts, frontend/src/main.tsx</files>
  <action>Ensure the theme store uses the same localStorage key and class logic as the bootstrap. In `main.tsx`, import a tiny bootstrap initializer (or call a store initializer) before `createRoot(...).render(...)` to keep runtime consistent and to remove the `no-transition` class after initial paint (e.g., `requestAnimationFrame`).</action>
  <verify>Reload the app and confirm `no-transition` is removed after first paint and the theme remains consistent.</verify>
  <done>Bootstrap and store initialization are consistent, and `no-transition` is cleared after initial render.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal root theme overrides for visible change</name>
  <files>frontend/src/index.css</files>
  <action>Add minimal `html.light` and `html.dark` root/background/text overrides to ensure a visible change when toggling, without doing full token migration. Keep this scoped and clearly separated so Phase 2 can replace it with semantic tokens.</action>
  <verify>Toggle theme and confirm background/text color change is perceptible on auth and dashboard routes.</verify>
  <done>Light/dark toggle produces a visible change with minimal root overrides.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] Manual: reload on /login and /dashboard with saved theme set to light and dark, confirm no flash
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Theme preference persists across reloads
- No visible startup flash on auth or dashboard routes
</success_criteria>

<output>
After completion, create `.planning/phases/01-theme-runtime-foundation/01-01-SUMMARY.md`
</output>
