---
phase: 01-theme-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [frontend/src/presentation/components/shared/dashboard/TopBar.tsx, frontend/src/presentation/pages/shared/SettingsPage.tsx, frontend/src/shared/store/useThemeStore.ts]
autonomous: true
requirements: [THM-02]
user_setup: []

must_haves:
  truths:
    - "Users can toggle theme from the top bar and Settings, and both controls stay in sync."
    - "Toggling theme immediately updates the root theme class and visible UI." 
  artifacts:
    - path: "frontend/src/presentation/components/shared/dashboard/TopBar.tsx"
      provides: "Dashboard-wide quick theme toggle control"
    - path: "frontend/src/presentation/pages/shared/SettingsPage.tsx"
      provides: "Labeled theme toggle in Settings"
  key_links:
    - from: "frontend/src/presentation/components/shared/dashboard/TopBar.tsx"
      to: "frontend/src/shared/store/useThemeStore.ts"
      via: "toggleTheme or setTheme"
      pattern: "useThemeStore"
    - from: "frontend/src/presentation/pages/shared/SettingsPage.tsx"
      to: "frontend/src/shared/store/useThemeStore.ts"
      via: "toggleTheme or setTheme"
      pattern: "useThemeStore"
---

<objective>
Add top-bar and Settings theme controls wired to the shared store so users can explicitly switch between light and dark.

Purpose: Satisfy THM-02 with consistent controls across dashboard and settings.
Output: Top bar icon toggle and settings labeled toggle bound to the same store.
</objective>

<execution_context>
@C:/Users/Christian Dave Vilan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Christian Dave Vilan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-theme-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-theme-runtime-foundation/01-RESEARCH.md

@frontend/src/presentation/components/shared/dashboard/TopBar.tsx
@frontend/src/presentation/pages/shared/SettingsPage.tsx
@frontend/src/shared/store/useThemeStore.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add top-bar quick theme toggle</name>
  <files>frontend/src/presentation/components/shared/dashboard/TopBar.tsx</files>
  <action>Insert a compact sun/moon icon button in the top bar action area. Read current theme from `useThemeStore`, and call `toggleTheme` on click. Add accessible label (e.g., `aria-label="Toggle theme"`) and a subtle active-state affordance without reworking layout.</action>
  <verify>Open any dashboard route and confirm the icon button is visible and toggles theme immediately.</verify>
  <done>Top-bar toggle is present across dashboard routes and updates theme state.</done>
</task>

<task type="auto">
  <name>Task 2: Add Settings labeled theme control</name>
  <files>frontend/src/presentation/pages/shared/SettingsPage.tsx</files>
  <action>Reuse the existing `Toggle` component to add a labeled theme switch in the preferences section. Bind the toggle to the shared store so it stays in sync with the top-bar control. Include short helper text if the Settings section style supports it.</action>
  <verify>Use the Settings toggle and confirm it updates the top-bar state and root theme class.</verify>
  <done>Settings toggle is labeled, accessible, and synchronized with the top bar.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm shared store wiring and avoid duplicate sources</name>
  <files>frontend/src/shared/store/useThemeStore.ts</files>
  <action>If needed, adjust or export selectors/helpers so both controls read from the same store shape. Avoid creating a second theme state in component local state.</action>
  <verify>Both toggles always reflect the same theme state after switching from either control.</verify>
  <done>Single source of truth is enforced for theme state.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] Manual: toggle from top bar, then from Settings, confirm immediate visual change and state sync
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Top bar and Settings toggles stay in sync and switch theme immediately
</success_criteria>

<output>
After completion, create `.planning/phases/01-theme-runtime-foundation/01-02-SUMMARY.md`
</output>
